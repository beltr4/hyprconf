I need your help to adjust some rust code, I'll send you the code, adjust it and send it back to me, no need to explain the file. Adjusting includes: moving all use statements to the top of the file, ordering them by standard, packages, internal, removing duplicate entries, removing empty lines, re-indenting where necessary and sorting functions by group/name. Please also check for unnecessary uses, such as the model being defined in the same file, and any missing uses. here's the first:
